on:
  push:
    branches:
      - main
  schedule:
    - cron: "0 2 * * *"
# jobs:
#   checkin:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkin
#       with:
#         python-version: 3.7
#         run: |
#           python main.py
#         shell: python
# name: yes24_crawler
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.7
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r BeautifulSoup; fi
      - name: Run main.py
        run: |
          python main.py
        env:
          SOCKBOOM_USER: ${{ secrets.SOCKBOOM_USER }}
          SOCKBOOM_PASSWD: ${{ secrets.SOCKBOOM_PASSWD }}
          SOCKBOOM_URL: ${{ secrets.SOCKBOOM_URL }}
          WEB_HOOK: ${{ secrets.WEB_HOOK }}
